@{
    Layout = "~/Views/Shared/_LoginLayout.cshtml";
    ViewData["Title"] = "Login";

}
@* <h2>Login</h2>
<form method="post">
    <input type="text" name="username" class="form-control form-control-lg" style="width: 300px; height: 40px;" placeholder="Tên đăng nhập" />
    <input type="password" name="password" class="form-control form-control-lg" style="width: 300px; height: 40px;" placeholder="Mật khẩu" />
    <button type="submit" style="width: 80px; height: 40px;">Login</button>

     <hr />

    <button type="submit" name="loginAsGuest" value="true">Login as guest</button>
</form> *@

@* <button type="submit" name="loginAsGuest" value="true">Login as guest</button> *@

<section class="ftco-section">
		<div class="container">
			<div class="row justify-content-center">
				<div class="col-md-6 text-center mb-5">
					<h2 class="heading-section">Login</h2>
				</div>
			</div>
			<div class="row justify-content-center">
				<div class="col-md-6 col-lg-4">
					<div class="login-wrap p-0">
		      	<h3 class="mb-4 text-center">Have an account?</h3>
		      	<form method="post" asp-action="Login" asp-controller="Account" class="signin-form" id="login-form">
		      		<div class="form-group">
		      			<input type="text" name="username" class="form-control" placeholder="Username" required>
		      		</div>
                    <div class="form-group">
                        <input id="password-field" name="password" type="password" class="form-control" placeholder="Password" required>
                        <span toggle="#password-field" class="fa fa-fw fa-eye field-icon toggle-password"></span>
                    </div>
                    <div class="form-group">
                        <button type="submit" class="form-control btn btn-primary submit px-3">Sign In</button>
                    </div>
                    <div class="form-group d-md-flex">
                        <div class="w-50">
                            <label class="checkbox-wrap checkbox-primary">Remember Me
                                <input type="checkbox" checked>
                                <span class="checkmark"></span>
                            </label>
                        </div>
                        <div class="w-50 text-md-right">
                            <a href="#" style="color: #fff">Forgot Password</a>
                        </div>
                    </div>
                    <div class="form-group">
		      			<input type="hidden" name="returnUrl" value="@Context.Request.Query["returnUrl"]" />
                        <input type="hidden" name="loginAsGuest" id= "loginAsGuest" value="false" />
		      		</div>
	          </form>
                <a href="#" id="guestLoginButton">
                    <p class="w-100 text-center text-white">&mdash; Or Sign In As Guest &mdash;</p>
                </a>
                @* <div class="social d-flex text-center">
                    <a href="#" class="px-2 py-2 mr-md-1 rounded"><span class="ion-logo-facebook mr-2"></span> Facebook</a>
                    <a href="#" class="px-2 py-2 ml-md-1 rounded"><span class="ion-logo-twitter mr-2"></span> Twitter</a>
                </div> *@
			</div>
		</div>
	</section>

@if (ViewBag.Error != null)
{
    <p style="color:red">@ViewBag.Error</p>
}

<script>
    document.getElementById('guestLoginButton').addEventListener('click', function (e) {
        e.preventDefault();

        document.getElementById('loginAsGuest').value = "true";
        document.getElementById('login-form').submit();
    })
</script>
